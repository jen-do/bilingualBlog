<h1>blog</h1>


{{#each postsEn}}
    <div class="project-container">
        <h2>{{en_title}}</h2>
        <p>{{en_post}}</p>
    </div>
    
{{/each}}


{{#each postsDe}}
    <div class="project-container">
        <h2>{{de_title}}</h2>
        <p>{{de_post}}</de>
    </div>
{{/each}}
