<h1>edit posts</h1>

{{#if err}}
    <p><strong>Something went wrong. Please try again.</strong></p>
{{/if}}

<form method="POST" autocomplete="off" enctype="multipart/form-data">
    <div id="form-outer-container">
    <div class="form-inner-container">
        <h2>DE</h2>
        <label><input type="text" name="de_title">Überschrift</label>
        <label><textarea name="de_post"></textarea>Text</label>
        <label>
            <select name="de_tags">
                <option value="event">Veranstaltung</option>
                <option value="news">Neuigkeiten</option>
            </select>
        Tags</label>
    </div>
    <div class="form-inner-container">
        <h2>EN</h2>
        <label><input type="text" name="en_title">heading</label>
        <label><textarea name="en_post"></textarea>text</label>
        <label>
            <select name="en_tags">
                <option value="event">event</option>
                <option value="news">news</option>
            </select>
        Tags</label>
    </div>
    <div class="form-inner-container">
    <label><input type="file" accept="image/*" name="file" ></label>
    <input type="hidden" name="_csrf" value="{{csrfToken}}">
    <button>submit</button>
    </div>
</div>

</form>
